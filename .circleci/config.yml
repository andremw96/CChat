version: 2.1

jobs:
  build:
    docker:
      - image: circleci/android:api-29-node
    steps:
      - checkout
      - restore_cache:
          key: jars-{{ checksum "build.gradle" }}-{{ checksum  "app/build.gradle" }}
      - run:
          name: Download Dependencies
          command: ./gradlew :app:dependencies
      - save_cache:
          paths:
            - ~/.gradle
          key: jars-{{ checksum "build.gradle" }}-{{ checksum  "app/build.gradle" }}
      - run:
          name: Jacoco Run Unit Tests
          command: ./gradlew testDebugUnitTest
      - run:
          name: Analyze on SonarCloud
          command: ./gradlew clean sonarqube -Dsonar.login=7196af5e23a58bfb56714c254f47dcb62078c97e
      - store_artifacts:
          path: app/build/reports
          destination: reports
      - store_test_results:
          path: app/build/test-results

workflows:
  main:
    jobs:
      - build:
          context: SonarCloud
